apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backstage.fullname" . }}-app-config
  namespace: {{ .Release.Namespace }}
data:
  app-config.yaml: {{ required "A valid .Values.appConfig entry required!" .Values.appConfig | indent 4 }}

